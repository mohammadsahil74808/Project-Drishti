# scripts/

Developer utility scripts (e.g. `seed_db.sh`, `reset_dev_env.sh`, `run_migrations.sh`)
added as the team needs them during implementation. Keeps one-off ops commands out
of README prose and version-controlled.
