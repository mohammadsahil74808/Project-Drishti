# Architecture Docs

Place system architecture diagrams, ER diagrams, and sequence diagrams here as the
project develops (`.png`/`.svg` exports + editable source if using diagrams.net/Excalidraw —
both free tools).

Nothing here yet — architecture diagram will be added once backend schema (Phase 2) is finalized.
