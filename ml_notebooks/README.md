# ml_notebooks/

Jupyter notebooks for exploratory data analysis and model experimentation
(forecasting, risk scoring, clustering) before logic is hardened into
`backend/app/ai/`. Keeping this separate from production code lets the team
iterate on models quickly without touching the API, and gives judges visibility
into your ML process/rigor.
