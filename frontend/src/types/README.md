# types/

Shared TypeScript types/interfaces mirroring backend Pydantic schemas (e.g.
`CrimeRecord`, `RiskScore`, `Hotspot`) — kept manually in sync for now; codegen from
the FastAPI OpenAPI spec can be added later as a `chore`.
