# components/assistant/

AI Assistant chat UI — streaming responses over WebSocket, with inline chart/map
rendering when the backend agent returns structured data. Built in the AI assistant
feature phase.
