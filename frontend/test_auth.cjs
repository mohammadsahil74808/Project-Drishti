const { chromium } = require('playwright'); (async () => { const browser = await chromium.launch({ headless: true }); const page = await browser.newPage(); page.on('request', req => { if (req.url().includes('/api/v1/')) console.log('REQ:', req.url(), req.headers()); }); page.on('response', res => { if (res.url().includes('/api/v1/')) console.log('RES:', res.url(), res.status()); }); await page.goto('http://localhost:5173/login'); await page.fill('input[type=text]', 'ADMIN001'); await page.fill('input[type=password]', 'Admin@123'); await page.click('button[type=submit]'); await page.waitForTimeout(3000); await browser.close(); })();
