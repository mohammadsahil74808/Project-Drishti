# app/

Application package root. `main.py` creates and configures the FastAPI instance.
Every subfolder here is a distinct architectural layer — see `backend/README.md` for the
full breakdown and rationale.
