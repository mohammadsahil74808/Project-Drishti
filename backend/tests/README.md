# tests/

Pytest suite. Mirrors the `app/` structure (e.g. `test_services/`, `test_api/`,
`test_ai/`) as it grows. Minimum bar per feature: one happy-path + one failure-path
test. Run with `pytest` from `backend/`.
